name: Keep CookaVel Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Runs every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping CookaVel
        run: curl -s https://cookavel.onrender.com > /dev/null
